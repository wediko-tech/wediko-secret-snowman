.row
  .col-xs-12
    .wishlist-header Wediko Gift Exchange
    .wishlist-page
      - if @events.any?
        .event-list
          = render partial: "event_block", collection: @events, as: :event
      - else
        h2 There are no active events currently. Try back later!
